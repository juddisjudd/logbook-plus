name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-windows:
    permissions:
      contents: write
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build frontend
        run: bun run build

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Logbook+ ${{ github.ref_name }}'
          releaseBody: 'Logbook+ ${{ github.ref_name }} - Windows release'
          releaseDraft: false
          prerelease: false
          args: --target x86_64-pc-windows-msvc

      - name: Generate latest.json for updater
        run: |
          $version = "${{ github.ref_name }}".substring(1)
          $tag = "${{ github.ref_name }}"

          $latestJson = @{
              version = $version
              notes = "Logbook+ $tag - Windows release"
              pub_date = (Get-Date -AsUTC -Format "o")
              platforms = @{
                  "windows-x86_64" = @{
                      signature = ""
                      url = "https://github.com/juddisjudd/logbook-plus/releases/download/$tag/logbook-plus_${version}_x64-setup.msi"
                  }
              }
          }

          $latestJson | ConvertTo-Json | Out-File -FilePath "latest.json" -Encoding UTF8

      - name: Upload latest.json to release
        uses: softprops/action-gh-release@v1
        with:
          files: latest.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
